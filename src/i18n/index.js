import en from './en';
import de from './de';

/**
 * Available languages, to add a new language, just copy one of the existing
 * language files, translate the strings, import it here and add it to the
 * messages object below.
 */
const messages = {
    en: en,
    de: de,
}

export default messages;